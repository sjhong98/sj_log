const HEADER_HEIGHT = 50

/** 메인 콘텐츠 영역 패딩 (px) - 미디어 쿼리 대신 useMediaQuery와 함께 사용 */
export const CONTENT_PADDING_MOBILE = 0
export const CONTENT_PADDING_DESKTOP = 20
export const INNER_CONTENT_HEIGHT = `calc(100vh - var(--header-height) - var(--content-inner-padding))`
