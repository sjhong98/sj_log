# Supabase 설정
# 서버에서 사용하는 환경 변수 (클라이언트에서 접근 불가)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-service-role-key

# 클라이언트에서 사용하는 환경 변수 (브라우저에서 접근 가능)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_KEY=your-supabase-anon-key

# 주의사항:
# 1. .env.local 파일을 생성하고 실제 값으로 채워주세요
# 2. .env.local은 .gitignore에 포함되어 있어 커밋되지 않습니다
# 3. NEXT_PUBLIC_ 접두사가 붙은 변수는 클라이언트에서 접근 가능합니다
# 4. 서버 액션에서는 SUPABASE_KEY (service role)을 사용합니다
# 5. 클라이언트에서는 NEXT_PUBLIC_SUPABASE_KEY (anon key)를 사용합니다
